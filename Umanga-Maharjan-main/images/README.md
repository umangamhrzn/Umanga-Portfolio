Place project photos here as separate image files.

Steps:
1. Save the Kalpa photo as `kalpa.jpg`, the Industrial Experience Project photo as `IEP.jpg`, and the Rotaract photo as `Rotaract.jpg`.
2. Put all files in the `images/` directory relative to the project root.
3. Open `projects.html` to confirm the images appear in the project tiles.

If you want, I can add the actual image file if you upload it into the repository or give me the binary to write.